# C4 Architecture — GCP-Drone-Comms-Unit

C4 model (Context → Container → Component) for the drone-comms / mesh-SA → TAK bridge.
Diagrams are Mermaid so they render on GitHub. See [ARCHITECTURE.md](ARCHITECTURE.md) for
the framework's design rationale, [CHARTER.md](CHARTER.md) for the stable scope/invariants,
and [ROADMAP.md](ROADMAP.md) for the long-term milestone trajectory.

## Level 1 — System Context

```mermaid
flowchart LR
    operator["Operator / Warfighter<br/>(ATAK on phone/tablet)"]
    autopilot["MAVLink autopilot<br/>(ArduPilot / PX4)"]
    fc["Betaflight FC<br/>(MSP over serial)"]
    mesh["Mesh peers<br/>(Meshtastic / HaLow / IP)"]
    tak["TAK server<br/>(FreeTAKServer)"]

    unit(["GCP-Drone-Comms-Unit<br/>(Jetson edge node)"])

    autopilot -->|MAVLink| unit
    fc -->|MSP| unit
    mesh <-->|versioned SA envelopes| unit
    unit -->|CoT over TCP/multicast| tak
    unit -->|CoT multicast SA| operator
    tak --> operator
```

The unit ingests drone/FC telemetry and mesh SA, and emits **CoT** so ATAK clients see
positions as tracks. ATAK itself runs on phones/tablets, not on the unit.

## Level 2 — Container

```mermaid
flowchart TB
    subgraph unit["GCP-Drone-Comms-Unit (Jetson)"]
        gateway["meshsa gateway process<br/>(flightctl/run_gateway.py)"]
        meshsa["meshsa framework<br/>(Python package)"]
        fts["FreeTAKServer<br/>(Python 3.11 venv)"]
        proxy["mavp2p<br/>(MAVLink router)"]
        gateway --> meshsa
        proxy -->|udp 14550| gateway
        gateway -->|CoT tcp 8087| fts
    end
    autopilot["Autopilot"] -->|serial| proxy
    fc["Betaflight FC"] -->|serial MSP| gateway
    radio["Meshtastic radio"] -->|serial| gateway
    fts -->|CoT tcp/ssl| atak["ATAK clients"]
    gateway -->|CoT multicast 239.2.3.1:6969| atak
```

Containers are independent processes (systemd units): the **gateway** (meshsa node from a
JSON config), **FreeTAKServer**, and the **mavp2p** MAVLink router. Each scales/fails
independently; the gateway is the only one that must understand both sides.

## Level 3 — Component (inside the meshsa gateway)

```mermaid
flowchart TB
    subgraph node["meshsa Node (build_node from config)"]
        router["Router<br/>(dedupe by msg_id,<br/>per-transport codec, bridge)"]
        subgraph sources["Source transports (receive-only)"]
            mav["mavlink_source<br/>(pymavlink reader thread)"]
            msp["msp_source<br/>(YAMSPy reader thread)"]
            crsf["crsf_source<br/>(ELRS CRSF GPS reader)"]
            mtx["meshtastic / loopback"]
        end
        subgraph sinks["Sink transports"]
            taktcp["tak_tcp"]
            takmc["tak_multicast"]
        end
        subgraph codecs["Codecs (registry)"]
            tel["telemetry<br/>(struct → PLI Envelope)"]
            cot["cot<br/>(Envelope ↔ CoT XML,<br/>air pli_type via codec_options)"]
            jsonc["json / compact"]
        end
        mav --> router
        msp --> router
        crsf --> router
        mtx --> router
        router --> taktcp
        router --> takmc
        router -. decodes with .-> tel
        router -. encodes with .-> cot
    end
```

Key contracts (Python `Protocol`s, dependency-injected so everything tests without
hardware): `Transport` (start/stop/send/stream bytes), `Codec` (encode/decode `Envelope`),
`Clock`, `IdFactory`. New mediums plug in via `transport_registry` / `codec_registry` with
**no core edits**. The stateful MAVLink/MSP parse lives in each source transport's reader
thread; the `telemetry` codec is a stateless per-frame map. Drone tracks reuse the `PLI`
kind with an **air** `pli_type` configured per-transport — no `schema_version` bump.

**Adjunct services (opt-in, out of the hot path):** the node exposes an optional aiohttp
**health/metrics listener** (`/healthz`, `/metrics` in Prometheus or JSON); the `meshsa.fpv`
ground-side subsystem runs its own CRSF link-health monitor, flight logger, and camera
capture writer; and a **read-only** `meshsa.llm` situational-awareness assistant answers
operator questions over live telemetry (via mavlink2rest on `:8088`) and TAK tracks. These
issue no vehicle commands — see [CHARTER.md](CHARTER.md) §3 and the ratified, M2-gated
supervised-commanding initiative in [ROADMAP.md](ROADMAP.md).

## Data flow (one drone fix)

```mermaid
sequenceDiagram
    participant A as Autopilot
    participant P as mavp2p
    participant M as mavlink_source
    participant R as Router
    participant C as cot codec (air)
    participant F as FreeTAKServer
    participant T as ATAK
    A->>P: GLOBAL_POSITION_INT
    P->>M: udp:14550
    M->>R: ingest telemetry frame (unique msg_id)
    R->>R: telemetry.decode → Envelope(PLI)
    R->>C: encode for tak_tcp
    C->>F: CoT <event type="a-f-A-..."> on :8087
    F->>T: broadcast CoT track
```
